name: Deploy to Hetzner

on:
  push:
    branches:
      - main
  
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm'
        required: false
        default: 'deploy'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 46.225.75.191
          username: deploy
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            cd /opt/paramedic-care
            
            # Pull latest code
            git fetch origin
            git reset --hard origin/main
            
            # Remove emergentintegrations (not available on production)
            sed -i '/emergentintegrations/d' backend/requirements.txt
            
            # Stop containers but KEEP volumes (don't use 'down -v')
            sudo docker-compose stop
            
            # Remove only containers, not volumes
            sudo docker-compose rm -f
            
            # Rebuild and start (volumes are preserved)
            sudo docker-compose up -d --build
            
            # Update nginx config
            sudo tee /etc/nginx/sites-available/paramedic-care018 > /dev/null << 'NGINXCONF'
            server {
                listen 80;
                server_name paramedic-care018.rs www.paramedic-care018.rs;
                return 301 https://$server_name$request_uri;
            }

            server {
                listen 443 ssl;
                server_name paramedic-care018.rs www.paramedic-care018.rs;

                ssl_certificate /etc/letsencrypt/live/paramedic-care018.rs/fullchain.pem;
                ssl_certificate_key /etc/letsencrypt/live/paramedic-care018.rs/privkey.pem;

                gzip on;
                gzip_vary on;
                gzip_min_length 256;
                gzip_proxied any;
                gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml image/svg+xml;
                gzip_comp_level 6;

                location / {
                    proxy_pass http://localhost:3000;
                    proxy_http_version 1.1;
                    proxy_set_header Upgrade $http_upgrade;
                    proxy_set_header Connection 'upgrade';
                    proxy_set_header Host $host;
                    proxy_set_header X-Real-IP $remote_addr;
                    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                    proxy_set_header X-Forwarded-Proto $scheme;
                    proxy_cache_bypass $http_upgrade;
                }

                location /api {
                    proxy_pass http://localhost:8001;
                    proxy_http_version 1.1;
                    proxy_set_header Upgrade $http_upgrade;
                    proxy_set_header Connection 'upgrade';
                    proxy_set_header Host $host;
                    proxy_set_header X-Real-IP $remote_addr;
                    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                    proxy_set_header X-Forwarded-Proto $scheme;
                    proxy_read_timeout 300s;
                    client_max_body_size 50M;
                }

                location /ws {
                    proxy_pass http://localhost:8001;
                    proxy_http_version 1.1;
                    proxy_set_header Upgrade $http_upgrade;
                    proxy_set_header Connection "upgrade";
                    proxy_set_header Host $host;
                    proxy_read_timeout 86400;
                }
            }
            NGINXCONF
            
            sudo nginx -t && sudo systemctl reload nginx
            
            echo "Deployment completed at $(date)"
            echo "MongoDB data: PRESERVED"
            echo "Uploads data: PRESERVED"
